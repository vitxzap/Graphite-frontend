import { Flex, Button, Card, Center, Field, Stack, Input, } from "@chakra-ui/react";
import Header from "../../../components/header"

export default function Search() {
    return (
        <Flex minH="vh" maxW="vw" w="vw">
            <Header />
        </Flex>
    );    
}